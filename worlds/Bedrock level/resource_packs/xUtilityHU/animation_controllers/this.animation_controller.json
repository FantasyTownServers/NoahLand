{
  "format_version": "1.10.0",
  "animation_controllers": {
    "controller.animation.this": {
      "initial_state": "default",
      "states": {
        "default": {
          "transitions": [ { "id": "query.is_in_ui" } ]
        },
        "id": {
          "transitions": [
            { "default": "variable.this = 1.0; variable.use_item_type = query.is_using_item && query.get_equipped_item_name != 'bow' && query.get_equipped_item_name != 'fishing_rod' && query.get_equipped_item_name != 'crossbow' && query.get_equipped_item_name != 'trident'; return 0.0;" }
          ]
        }
      }
    }
  }
}
//made by ambient#2309 - do not redistribute publicly without permission!